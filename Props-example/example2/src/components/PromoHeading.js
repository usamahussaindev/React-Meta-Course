import React from "react";
import Promo from './components/Promo';

function PromoHeading(props) {
    return (
        <div>
            <h1>{props.Usama}</h1>
            <p>{props.Hussain}</p>
        </div>
    );
}